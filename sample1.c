#include <stdio.h>
#include <conio.h>

int main(){

// area of rectangle
// base * height

int area, base, height;

printf("\nInput the base: ");
scanf("%d", &base);

printf("\nInput the height: ");
scanf("%d", &height);

area = base * height;

printf("\nThe are of the rectangle is %2d", area);

getch();





}